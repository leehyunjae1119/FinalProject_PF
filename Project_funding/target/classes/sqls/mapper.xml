<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="pf_user">

	<select id="selectUser" resultType="PF_UserDto">
		SELECT * FROM PF_USER
		WHERE USER_ID = #{user_id}
	</select>
	
	<select id="insertUser" parameterType="PF_UserDto" >
		INSERT INTO PF_USER
		VALUES(PF_USER_SEQ.NEXTVAL, #{user_id}, #{user_pw}, #{user_email}, #{user_type}, NULL, 'FALSE', 'FALSE', NULL)
	</select>
	
	<select id="setEmail" parameterType="String">
		UPDATE PF_USER 
		SET USER_EMAIL_CHECK = 'TRUE' 
		WHERE USER_EMAIL = #{user_email}
	</select>
 
</mapper>